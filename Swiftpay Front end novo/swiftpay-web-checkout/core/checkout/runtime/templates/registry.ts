import { heroProTemplateModule } from '@/templates/hero-pro/module';
import type { CheckoutTemplateModule } from './types';

export const checkoutTemplateRegistry: CheckoutTemplateModule[] = [heroProTemplateModule];
